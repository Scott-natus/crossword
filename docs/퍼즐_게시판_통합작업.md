# 퍼즐, 게시판 통합작업

## 📋 작업 개요

### 요청사항
- **목표**: 8080(게시판)과 8081(퍼즐게임) Spring Boot 애플리케이션을 단일 애플리케이션(8080)으로 통합
- **도메인**: `natus250601.viewdns.net`
- **라우팅 구조**:
  - 메인 화면: `/` (기존 퍼즐게임 메인과 동일)
  - 게시판: `/board`
  - 퍼즐게임: `/Korean/game`, `/K-pop`, `/K-movie`, `/K-Drama`, `/K-Culture`
  - 관리자: `/K-CrossWord/admin/`

### 기술 스택
- **Spring Boot**: 8080 포트 통합 서비스
- **PostgreSQL**: 기존 데이터베이스 유지
- **Apache**: 리버스 프록시 (80 → 8080)
- **Redis**: 세션 관리
- **Thymeleaf**: 템플릿 엔진

## 🔧 주요 작업 내역

### 1. Spring Boot 애플리케이션 통합

#### 1.1 엔티티 및 리포지토리 통합
- **User 엔티티**: `crossword-projects` → `java-projects`로 복사
- **UserPuzzleGame 엔티티**: 퍼즐게임 관련 엔티티 통합
- **UserRepository**: 사용자 관리 리포지토리 통합
- **UserPuzzleGameRepository**: 퍼즐게임 데이터 관리

#### 1.2 서비스 레이어 통합
- **UserPuzzleGameService**: 퍼즐게임 서비스 로직 통합
- **AuthController**: 인증 상태 확인 API (`/api/auth/status`)
- **PuzzleApiController**: 퍼즐게임 API (`/api/puzzle/{theme}`)

### 2. 라우팅 구조 변경

#### 2.1 컨트롤러 라우팅
```java
// 메인 화면
@GetMapping("/") → static/index.html 서빙

// 퍼즐게임 화면
@GetMapping("/Korean/game") → static/index.html 서빙
@GetMapping("/K-pop") → static/index.html 서빙
@GetMapping("/K-movie") → static/index.html 서빙
@GetMapping("/K-Drama") → static/index.html 서빙
@GetMapping("/K-Culture") → static/index.html 서빙

// 게시판
@GetMapping("/board") → 게시판 메인

// 관리자
@GetMapping("/K-CrossWord/admin") → 관리자 패널
```

#### 2.2 API 엔드포인트
```java
// 인증 API
GET /api/auth/status → 인증 상태 확인

// 퍼즐게임 API
GET /api/puzzle/{theme} → 테마별 퍼즐 데이터
GET /api/puzzle/{theme}/hint → 힌트 조회
POST /api/puzzle/{theme}/check → 정답 확인
```

### 3. Spring Security 설정

#### 3.1 인증 규칙
```java
.authorizeHttpRequests(authz -> authz
    .requestMatchers("/", "/puzzle", "/css/**", "/js/**", "/images/**", "/webjars/**", "/favicon.ico").permitAll()
    .requestMatchers("/Korean/**", "/K-pop/**", "/K-movie/**", "/K-Drama/**", "/K-Culture/**").permitAll()
    .requestMatchers("/board/**").permitAll()
    .requestMatchers("/api/puzzle/**", "/api/auth/**").permitAll()
    .requestMatchers("/K-CrossWord/admin/**").authenticated()
    .anyRequest().authenticated()
)
```

#### 3.2 CORS 설정
```java
@CrossOrigin(origins = "*")
@RestController
@RequestMapping("/api/auth")
public class AuthController {
    // CORS 문제 해결을 위한 설정
}
```

### 4. Apache 프록시 설정

#### 4.1 통합 프록시 설정
```apache
<VirtualHost *:80>
    ServerName natus250601.viewdns.net
    
    # 통합된 Spring Boot 애플리케이션 (8080) - 모든 요청 처리
    ProxyPreserveHost On
    
    # Laravel API Service (9090) - 기존 유지
    ProxyPass /api/laravel/ http://127.0.0.1:9090/api/
    ProxyPassReverse /api/laravel/ http://127.0.0.1:9090/api/
    
    # Laravel Service (9000) - 기존 유지  
    ProxyPass /LaLavel/ http://127.0.0.1:9000/
    ProxyPassReverse /LaLavel/ http://127.0.0.1:9000/
    
    # 통합된 Spring Boot 애플리케이션 (8080) - 모든 요청 처리
    ProxyPass / http://127.0.0.1:8080/
    ProxyPassReverse / http://127.0.0.1:8080/
</VirtualHost>
```

### 5. 프론트엔드 통합

#### 5.1 메인 화면 (static/index.html)
- **기존 퍼즐게임 메인 화면과 동일한 UI**
- **게스트로 시작** 버튼: `/Korean/game`으로 이동
- **로그인 후**: 게임시작 버튼 표시
- **인증 상태 확인**: `/api/auth/status` API 호출

#### 5.2 JavaScript 인증 로직
```javascript
// 인증 상태 확인
function checkAuthStatus() {
    fetch('/api/auth/status', {
        method: 'GET',
        credentials: 'include'
    })
    .then(response => response.json())
    .then(data => {
        if (data.authenticated && data.userType === 'user') {
            // 인증된 사용자 - 게임시작 버튼 표시
            userButtons.style.setProperty('display', 'flex', 'important');
        } else {
            // 비인증 사용자 - 기본 버튼 표시
            guestButtons.style.setProperty('display', 'flex', 'important');
        }
    })
    .catch(error => {
        // 오류 발생 시 비인증 상태로 처리
        guestButtons.style.setProperty('display', 'flex', 'important');
    });
}
```

## 🐛 해결된 문제들

### 1. 컴파일 오류
- **문제**: Lombok 의존성 누락
- **해결**: `build.gradle`에 Lombok 의존성 추가
- **문제**: 엔티티 import 경로 오류
- **해결**: 패키지 경로 수정 (`com.example.crossword` → `com.example.board`)

### 2. 라우팅 충돌
- **문제**: `@GetMapping("/")` 중복 매핑
- **해결**: `HomeController`와 `PuzzleController` 라우팅 분리

### 3. API 응답 구조 불일치
- **문제**: JavaScript가 기대하는 `userType` 필드 누락
- **해결**: `AuthController` 응답에 `userType` 필드 추가

### 4. CORS 문제
- **문제**: 브라우저에서 API 호출 시 CORS 오류
- **해결**: `@CrossOrigin(origins = "*")` 어노테이션 추가

### 5. Spring Boot 애플리케이션 시작 실패
- **문제**: 애플리케이션 시작 후 즉시 종료
- **해결**: `nohup` 명령어로 백그라운드 실행 보장

## 📁 파일 구조

### 통합된 java-projects 구조
```
java-projects/
├── src/main/java/com/example/board/
│   ├── controller/
│   │   ├── AuthController.java          # 인증 API
│   │   ├── HomeController.java          # 메인 화면
│   │   ├── PuzzleController.java        # 퍼즐게임 화면
│   │   ├── PuzzleApiController.java    # 퍼즐게임 API
│   │   └── BoardController.java         # 게시판
│   ├── entity/
│   │   ├── User.java                    # 사용자 엔티티
│   │   └── UserPuzzleGame.java          # 퍼즐게임 엔티티
│   ├── repository/
│   │   ├── UserRepository.java          # 사용자 리포지토리
│   │   └── UserPuzzleGameRepository.java # 퍼즐게임 리포지토리
│   ├── service/
│   │   └── UserPuzzleGameService.java   # 퍼즐게임 서비스
│   └── config/
│       └── SecurityConfig.java          # 보안 설정
├── src/main/resources/
│   ├── static/
│   │   └── index.html                   # 퍼즐게임 메인 화면
│   └── templates/
│       ├── puzzle/
│       │   ├── main.html                # 퍼즐게임 메인
│       │   └── admin.html               # 관리자 패널
│       └── board/
│           └── main.html                # 게시판 메인
└── build.gradle                         # 빌드 설정
```

## 🚀 배포 및 실행

### 1. 빌드 및 실행
```bash
cd /var/www/html/java-projects
./gradlew build
nohup ./gradlew bootRun --args='--server.port=8080' > /tmp/java-projects.log 2>&1 &
```

### 2. 서비스 확인
```bash
# 포트 확인
netstat -tlnp | grep 8080

# API 테스트
curl -X GET "http://localhost:8080/api/auth/status"
```

### 3. Apache 재시작
```bash
sudo systemctl restart apache2
```

## 📊 결과

### 성공적으로 통합된 기능들
- ✅ **메인 화면**: 기존 퍼즐게임 메인과 동일한 UI
- ✅ **게스트 모드**: 인증 없이 퍼즐게임 플레이 가능
- ✅ **로그인 기능**: 인증된 사용자용 게임시작 버튼
- ✅ **게시판**: 기존 게시판 기능 유지
- ✅ **관리자 패널**: 퍼즐게임 관리 기능
- ✅ **API 통합**: 모든 API가 8080 포트에서 통합 서비스

### 기술적 성과
- **포트 통합**: 8080, 8081 → 8080 단일 포트
- **코드 통합**: 두 개의 Spring Boot 애플리케이션 → 하나로 통합
- **데이터베이스**: 기존 PostgreSQL 스키마 유지
- **프론트엔드**: 기존 UI/UX 유지하면서 백엔드 통합

## 🔄 향후 작업

### 운영서버 배포
- 개발서버(222.100.103.227) → 운영서버(K-crossword.com) 배포
- 데이터베이스 백업 및 복원
- Apache 설정 이전
- 도메인 설정 변경

### 추가 기능 개발
- 테마별 퍼즐게임 데이터 관리
- 다국어 힌트 시스템
- 뉴스레터 및 알림 기능
- SNS 공유 기능

## 📊 2025-10-31 작업 내역: 데이터베이스 동기화 기능 구현 및 테이블 코멘트 관리

### 작업 개요
- 관리자 페이지에 개발서버와 운영서버 간 데이터베이스 동기화 기능 구현
- 테이블 목록에 테이블 코멘트(설명) 표시 기능 추가
- 테이블 동기화 시 코멘트도 함께 동기화하는 기능 구현
- 32개 테이블에 코멘트 추가

### 주요 구현 사항

#### 1. 데이터베이스 동기화 페이지 구현
**백엔드 구현:**
- `DatabaseSyncService.java`: 데이터베이스 동기화 서비스 클래스 생성
  - `getLocalTables()`: 로컬(개발) 서버 테이블 목록 조회
  - `getRemoteTables()`: 원격(운영) 서버 테이블 목록 조회
  - `syncTable()`: 테이블 동기화 메인 메서드
  - `getTableInfo()`: 테이블 정보 조회 (로우 개수, 최종 갱신일, 코멘트)
  - `createTableIfNotExists()`: 원격 서버에 테이블이 없으면 로컬 스키마로 생성
  - `getCreateTableStatement()`: PostgreSQL 스키마 기반 CREATE TABLE 문 동적 생성
  - `syncTableComment()`: 테이블 코멘트 동기화 메서드
  - `getTableData()`: 테이블 데이터 조회
  - `insertDataToRemote()`: 원격 서버에 데이터 일괄 삽입

**API 엔드포인트:**
- `GET /admin/api/db-sync/local-tables`: 개발 서버 테이블 목록 조회
- `GET /admin/api/db-sync/remote-tables`: 운영 서버 테이블 목록 조회
- `POST /admin/api/db-sync/sync/{tableName}`: 테이블 동기화 실행

**프론트엔드 구현:**
- `static/admin/db-sync/index.html`: 데이터베이스 동기화 관리 페이지
  - 좌측: 개발 서버 테이블 목록
  - 우측: 운영 서버 테이블 목록
  - 테이블 선택 후 동기화 버튼으로 실행
  - 테이블명, 로우 개수, 최종 갱신일, 테이블 코멘트 표시

#### 2. 테이블 코멘트 표시 기능 추가
**구현 내용:**
- 개발 서버 테이블 목록에 테이블 코멘트 표시
- 표시 형식: `테이블명 (코멘트)` 또는 코멘트가 없으면 `테이블명`만 표시
- `getTableInfo()` 메서드에서 `obj_description()` 함수로 코멘트 조회

**데이터베이스 코멘트 추가:**
- 32개 테이블에 코멘트 추가 완료
- 주요 테이블 코멘트:
  - `pz_words`: 퍼즐 단어 테이블 - 십자낱말 퀴즈에 사용되는 단어 저장
  - `pz_hints`: 퍼즐 힌트 테이블 - 퍼즐 단어에 대한 힌트 저장
  - `puzzle_levels`: 퍼즐 레벨 테이블 - 레벨별 난이도, 단어 수, 교차점 수, 시간 제한 등 설정
  - `boards`: 게시판 테이블 - 게시글 저장
  - `users`: 사용자 테이블 - 시스템 사용자 정보 저장
  - 기타 27개 테이블 코멘트 추가

#### 3. 인코딩 문제 해결
**문제:**
- 테이블 코멘트가 깨져서 표시됨 (예: `⑥ 蹂`)
- PostgreSQL JDBC 연결 시 UTF-8 인코딩 미지정

**해결 방법:**
- JDBC URL에 인코딩 파라미터 추가: `characterEncoding=utf8&useUnicode=true&stringtype=unspecified`
- 연결 생성 시 `SET client_encoding TO 'UTF8'` 명령 실행
- `application.properties`에 인코딩 설정 추가

#### 4. 테이블 동기화 시 코멘트 동기화 기능
**구현 내용:**
- `syncTableComment()` 메서드 추가
- 동기화 프로세스:
  1. 로컬 DB에서 테이블 코멘트 조회
  2. 원격 DB에 동일한 코멘트 적용
  3. 코멘트가 없으면 원격에서 제거
- SQL 인젝션 방지: 작은따옴표 이스케이프 처리 (`'` → `''`)
- 코멘트 동기화 실패해도 데이터 동기화는 계속 진행

#### 5. 동적 테이블 생성 기능
**구현 내용:**
- 원격 서버에 테이블이 없으면 자동 생성
- `getCreateTableStatement()`: PostgreSQL 스키마 기반 CREATE TABLE 문 동적 생성
  - 컬럼 정보 (이름, 데이터 타입, 길이, NULL 제약, 기본값)
  - PRIMARY KEY 제약조건
  - 시퀀스 생성 및 연결 (SERIAL 타입)
- PRIMARY KEY 조회 시 `key_column_usage` 사용 (이전 `constraint_column_usage` 오류 수정)

### 해결된 문제들

#### PRIMARY KEY 조회 오류
**문제**: `constraint_column_usage`에는 `ordinal_position` 컬럼이 없음
**해결**: `key_column_usage` 사용으로 변경

#### 인코딩 문제
**문제**: 테이블 코멘트가 깨져서 표시됨
**해결**: JDBC URL 인코딩 파라미터 추가 및 연결 시 UTF-8 설정

#### COMMENT ON TABLE 플레이스홀더 미지원
**문제**: PostgreSQL `COMMENT ON TABLE`은 PreparedStatement 플레이스홀더 미지원
**해결**: Statement 사용 및 SQL 인젝션 방지를 위한 이스케이프 처리

### 수정된 파일 목록

**백엔드 파일:**
- `java-projects/src/main/java/com/example/board/service/DatabaseSyncService.java` (신규)
- `java-projects/src/main/java/com/example/board/controller/AdminApiController.java` (수정 - 동기화 API 추가)
- `java-projects/src/main/java/com/example/board/controller/AdminController.java` (수정 - 동기화 페이지 추가)
- `java-projects/src/main/resources/application.properties` (수정 - 인코딩 설정 추가)

**프론트엔드 파일:**
- `java-projects/src/main/resources/static/admin/db-sync/index.html` (신규)
- `java-projects/src/main/resources/templates/admin/index.html` (수정 - 동기화 링크 추가)

**문서화:**
- `docs/TABLE_COMMENTS_ANALYSIS.md` (신규 - 테이블 코멘트 분석 문서)

### 테스트 결과

**기능 테스트:**
- ✅ 개발 서버 테이블 목록 조회 (코멘트 포함)
- ✅ 운영 서버 테이블 목록 조회
- ✅ 테이블 동기화 실행
- ✅ 테이블 코멘트 동기화 확인
- ✅ 동적 테이블 생성 확인

**인코딩 테스트:**
- ✅ 한글 코멘트 정상 표시 확인
- ✅ UTF-8 인코딩 정상 작동 확인

### 완성된 기능 목록

1. **데이터베이스 동기화 관리 페이지**: 개발/운영 서버 테이블 목록 조회
2. **테이블 코멘트 표시**: 개발 서버 테이블 목록에 코멘트 표시
3. **테이블 동기화**: 개발 서버 → 운영 서버 데이터 동기화
4. **코멘트 동기화**: 테이블 동기화 시 코멘트도 함께 동기화
5. **동적 테이블 생성**: 원격 서버에 테이블이 없으면 자동 생성
6. **테이블 코멘트 관리**: 32개 테이블에 코멘트 추가 완료

---

**작업 완료일**: 2025-10-23  
**작업자**: Cursor AI Assistant  
**상태**: ✅ 완료

---

## 2025-11-01 작업 내역: 관리자 화면 통계 수정 및 레벨관리 화면 개선

### 작업 내용

#### 1. 정제완료 통계 수정 (단어 관리, 퍼즐 관리 메인 화면)

**문제점**: 
- 정제완료 카드가 힌트가 있는 단어 수(`getWordsWithHintsCount()`)를 표시하고 있었음
- 실제로는 `conf_yn = 'Y'`인 단어 수를 표시해야 함
- 현재 실제 정제완료 단어는 225개인데 37,960개로 표시되고 있었음

**해결 방법**:
1. `PzWordRepository`에 `countByConfYn(String confYn)` 메서드 추가
   - Spring Data JPA 메서드 네이밍 규칙으로 자동 생성되는 쿼리: `SELECT COUNT(*) FROM pz_words WHERE conf_yn = ?`

2. `PzWordService`에 `getRefinedWordsCount()` 메서드 추가
   ```java
   public long getRefinedWordsCount() {
       return pzWordRepository.countByConfYn("Y");
   }
   ```

3. `AdminApiController`의 두 통계 API 엔드포인트 수정
   - `/admin/api/stats`: 시스템 전체 통계
   - `/admin/api/words/stats`: 단어 통계
   - 두 엔드포인트 모두 `getWordsWithHintsCount()` 대신 `getRefinedWordsCount()` 사용

**영향 받는 화면**:
- 퍼즐 관리 메인 화면 (`/admin/`)
- 단어 관리 화면 (`/admin/words`)

**쿼리**:
```sql
SELECT COUNT(*) FROM pz_words WHERE conf_yn = 'Y';
```

#### 2. AI 힌트 관리 메뉴의 전체힌트 통계 수정

**문제점**:
- `HintGeneratorManagementService.getStatistics()`에서 `total_hints`를 반환하지 않았음
- 프론트엔드에서 `data.total_hints`를 요청하지만 값이 없어 0 또는 undefined로 표시됨

**해결 방법**:
- `HintGeneratorManagementService.getStatistics()` 메서드에 다음 코드 추가:
  ```java
  long totalHints = pzHintRepository.count();
  stats.put("total_hints", totalHints);
  ```

**영향 받는 화면**:
- AI 힌트 관리 화면 (`/admin/hint-generator`)

**쿼리**:
```sql
SELECT COUNT(*) FROM pz_hints;
```

#### 3. 레벨관리 화면 통계 카드 제거

**문제점**:
- 레벨관리 화면에 불필요한 통계 카드(총 레벨 수, 평균 단어 수, 최대 단어 수, 최소 단어 수)가 표시됨
- 사용자가 통계 카드가 필요 없다고 요청

**해결 방법**:
1. HTML에서 통계 카드 섹션 제거 (167-193 라인)
2. JavaScript에서 `loadStats()` 함수 호출 제거
3. `loadStats()` 함수 정의 제거
4. 레벨 수정/삭제 후 `loadStats()` 호출 제거

**영향 받는 화면**:
- 레벨 관리 화면 (`/admin/levels`)

### 변경된 파일

1. **백엔드 (Java)**:
   - `java-projects/src/main/java/com/example/board/repository/PzWordRepository.java`
     - `countByConfYn(String confYn)` 메서드 추가
   
   - `java-projects/src/main/java/com/example/board/service/PzWordService.java`
     - `getRefinedWordsCount()` 메서드 추가
   
   - `java-projects/src/main/java/com/example/board/service/HintGeneratorManagementService.java`
     - `getStatistics()` 메서드에 `total_hints` 추가
   
   - `java-projects/src/main/java/com/example/board/controller/AdminApiController.java`
     - `/admin/api/stats` 엔드포인트에서 `getRefinedWordsCount()` 사용
     - `/admin/api/words/stats` 엔드포인트에서 `getRefinedWordsCount()` 사용

2. **프론트엔드 (HTML/JavaScript)**:
   - `java-projects/src/main/resources/static/admin/levels/index.html`
     - 통계 카드 HTML 섹션 제거
     - `loadStats()` 함수 및 모든 호출 제거

### 테스트 결과

- 정제완료 통계가 올바르게 표시되는지 확인 필요
- 전체힌트 통계가 올바르게 표시되는지 확인 필요
- 레벨관리 화면에서 통계 카드가 제거되었는지 확인 필요

---

**작업 완료일**: 2025-11-01  
**작업자**: Cursor AI Assistant  
**상태**: ✅ 완료
