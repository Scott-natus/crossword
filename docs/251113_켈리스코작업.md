# 2025-11-13 켈리스코 작업 내역

## 작업 개요
- OHRF 매출 추출 시스템의 UI/UX 개선 및 데이터 표시 순서 문제 해결

## 작업 일시
- 2025년 11월 13일

## 주요 작업 내용

### 1. 라디오 버튼 선택에 따른 입력 필드 Hide/Show 기능 수정

#### 문제점
- 라디오 버튼("메뉴별" / "일별") 선택 시 입력 필드가 제대로 숨겨지지 않거나 표시되지 않는 문제
- Bootstrap의 `d-flex` 클래스가 JavaScript로 설정한 `display` 속성을 덮어쓰는 CSS 우선순위 문제

#### 해결 방법
- **HTML 수정** (`index.html`):
  - `yearMonthInputs` div의 초기 스타일을 `display: none !important;`로 설정
  - `dateRangeButtons` div의 초기 스타일을 `display: flex !important;`로 설정
  
- **JavaScript 수정** (`toggleDateInputs()` 함수):
  - `style.display` 직접 설정 대신 `setAttribute('style', 'display: flex !important;')` 방식 사용
  - CSS 우선순위 문제를 해결하기 위해 `!important` 플래그 추가
  - 디버깅을 위한 console.log 제거 (최종 버전)

#### 수정 파일
- `/var/www/html/java-projects/src/main/resources/static/project/kalisco/crawling/saleSituation/index.html`

#### 수정 내용 상세
```javascript
function toggleDateInputs() {
    const saleType = document.querySelector('input[name="saleType"]:checked').value;
    const dateRangeInputs = document.getElementById('dateRangeInputs');
    const yearMonthInputs = document.getElementById('yearMonthInputs');
    const dateRangeButtons = document.getElementById('dateRangeButtons');
    
    if (saleType === 'menu') {
        // 메뉴별: 시작일/종료일 입력 필드와 날짜 버튼 표시, 연도/월 숨김
        if (dateRangeInputs) {
            dateRangeInputs.setAttribute('style', 'display: flex !important;');
        }
        if (yearMonthInputs) {
            yearMonthInputs.setAttribute('style', 'display: none !important;');
        }
        if (dateRangeButtons) {
            dateRangeButtons.setAttribute('style', 'display: flex !important;');
        }
    } else {
        // 일별: 연도/월만 표시, 시작일/종료일 입력 필드와 날짜 버튼 숨김
        if (dateRangeInputs) {
            dateRangeInputs.setAttribute('style', 'display: none !important;');
        }
        if (yearMonthInputs) {
            yearMonthInputs.setAttribute('style', 'display: flex !important;');
        }
        if (dateRangeButtons) {
            dateRangeButtons.setAttribute('style', 'display: none !important;');
        }
    }
}
```

### 2. 일별 매출 데이터 헤더 순서 문제 해결

#### 문제점
- 일별 매출 원본 페이지는 "일자, 매출액(원), 영수건수, 고객수" 순서로 표시
- 크롤링된 데이터가 다른 순서로 표시되는 문제
- `HashMap` 사용으로 인한 순서 보장 불가

#### 해결 방법
- **백엔드 수정** (`OurHomeScraperService.java`):
  - `HashMap` → `LinkedHashMap`으로 변경하여 삽입 순서 보장
  - 데이터 행을 Map에 넣을 때 헤더 순서대로 유지
  
- **프론트엔드 수정** (`index.html`):
  - `displayData()` 함수가 API 응답의 `headers` 배열을 받도록 수정
  - 헤더 순서를 API에서 받은 순서대로 사용
  - 원본 페이지 순서 유지

#### 수정 파일
- `/var/www/html/java-projects/src/main/java/com/example/board/service/OurHomeScraperService.java`
- `/var/www/html/java-projects/src/main/resources/static/project/kalisco/crawling/saleSituation/index.html`

#### 수정 내용 상세

**백엔드 (OurHomeScraperService.java)**:
```java
import java.util.LinkedHashMap;  // 추가

// 데이터 행 추출 시 LinkedHashMap 사용
Map<String, String> rowData = new LinkedHashMap<>();
int maxCols = Math.min(headers.size(), cells.size());
for (int j = 0; j < maxCols; j++) {
    String cellText = cells.get(j).getText().trim();
    String header = j < headers.size() ? headers.get(j) : "컬럼" + (j + 1);
    rowData.put(header, cellText);
}
```

**프론트엔드 (index.html)**:
```javascript
// API 호출 후 헤더 순서 전달
displayData(result.data || [], result.headers || []);

// displayData 함수 수정
function displayData(data, headerOrder) {
    // ...
    // 헤더 순서 결정: API에서 받은 headerOrder가 있으면 사용
    let headers;
    if (headerOrder && headerOrder.length > 0) {
        headers = headerOrder;  // API에서 받은 헤더 순서 사용
    } else {
        headers = Object.keys(data[0]);  // fallback
    }
    // ...
}
```

## 테스트 결과
- ✅ 라디오 버튼 선택 시 입력 필드가 정상적으로 숨겨지고 표시됨
- ✅ 일별 매출 데이터가 원본 페이지와 동일한 순서로 표시됨 (일자, 매출액(원), 영수건수, 고객수)

## 관련 파일 목록
1. `/var/www/html/java-projects/src/main/java/com/example/board/service/OurHomeScraperService.java`
2. `/var/www/html/java-projects/src/main/resources/static/project/kalisco/crawling/saleSituation/index.html`

## 참고 사항
- Bootstrap의 `d-flex` 클래스는 `display: flex`를 강제하므로, JavaScript로 동적 제어 시 `!important` 플래그가 필요함
- JavaScript의 `Object.keys()`는 삽입 순서를 보장하지만, JSON 직렬화/역직렬화 과정에서 순서가 바뀔 수 있으므로 API에서 명시적으로 헤더 순서를 전달하는 것이 안전함
- `LinkedHashMap`은 삽입 순서를 보장하므로, 헤더 순서가 중요한 경우 반드시 사용해야 함

## 추가 작업 내역 (2025-11-13 오후)

### 3. 엑셀 다운로드 기능 개선

#### 문제점
- 엑셀 다운로드 버튼 클릭 시 API를 호출하여 다시 크롤링하는 구조
- 사용자가 요청한 것은 이미 수집된 DataTable의 데이터만 엑셀로 변환하는 기능

#### 해결 방법
- **DataTable 기본 엑셀 다운로드 기능 사용**:
  - API 호출 제거
  - `dataTable.button('.buttons-excel').trigger()`로 현재 표시된 데이터만 엑셀로 변환
  - 재크롤링 없이 즉시 다운로드 가능

- **파일명 규칙 변경**:
  - 기존: 서버에서 생성한 파일명 사용
  - 변경: `기능별+일자` 형식으로 통일
    - 메뉴별: `메뉴별매출_YYYYMMDD.xlsx`
    - 일별: `일별매출_YYYYMMDD.xlsx`

- **DataTable 버튼 컨테이너 제거**:
  - `dom: 'Bfrtip'` → `dom: 'frtip'` (버튼 영역 'B' 제거)
  - `class="dt-buttons btn-group flex-wrap"` 영역이 DOM에 생성되지 않도록 수정
  - 기존 엑셀 다운로드 버튼은 그대로 유지하고 DataTable 기능만 사용

#### 수정 파일
- `/var/www/html/java-projects/src/main/resources/static/project/kalisco/crawling/saleSituation/index.html`

#### 수정 내용 상세

**DataTable 초기화**:
```javascript
dataTable = $('#dataTable').DataTable({
    // ... 기타 옵션 ...
    dom: 'frtip', // 'B' 제거하여 버튼 영역을 DOM에 생성하지 않음
    buttons: [
        {
            extend: 'excel',
            text: '<i class="fas fa-file-excel"></i>',
            title: filename,
            filename: filename,
            className: '',
            exportOptions: {
                format: {
                    body: function(data, row, column, node) {
                        return data;
                    }
                }
            }
        }
    ]
});

// 버튼 컨테이너가 생성되지 않도록 확인
setTimeout(function() {
    $('.dt-buttons').remove();
}, 100);
```

**downloadExcel() 함수 간소화**:
```javascript
function downloadExcel() {
    if (!dataTable) {
        alert('다운로드할 데이터가 없습니다. 먼저 데이터수집을 실행해주세요.');
        return;
    }
    
    // DataTable의 엑셀 버튼 클릭 트리거 (재크롤링 없이 현재 데이터만 사용)
    dataTable.button('.buttons-excel').trigger();
}
```

### 4. UI 텍스트 개선

#### 변경 사항
- "크롤링 중..." → "수집중..."
- "크롤링 실패" → "데이터 수집 실패"
- "크롤링 오류" → "데이터 수집 오류"

#### 브랜드명 변경
- "우리홈" → "아워홈" (title 태그)

#### 수정 파일
- `/var/www/html/java-projects/src/main/resources/static/project/kalisco/crawling/saleSituation/index.html`

## 테스트 결과
- ✅ 엑셀 다운로드가 재크롤링 없이 현재 DataTable 데이터만 사용하여 작동
- ✅ 파일명이 `기능별+일자` 형식으로 정상 생성 (예: `메뉴별매출_20251113.xlsx`)
- ✅ DataTable 버튼 컨테이너가 DOM에 생성되지 않음
- ✅ 기존 엑셀 다운로드 버튼이 정상 작동

## 다음 작업 예정
- 없음 (현재 요청사항 모두 완료)

