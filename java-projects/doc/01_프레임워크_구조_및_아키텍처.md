# 캘리스코 프로젝트 - 프레임워크 구조 및 아키텍처

## 📅 작성일: 2025-12-08
## 🏗️ 프로젝트명: 캘리스코 (Kalisco)

---

## 🎯 프로젝트 개요

### 프로젝트 정보
- **프로젝트명**: 캘리스코 (Kalisco)
- **프로젝트 경로**: `/var/www/html/project/kalisco/`
- **접속 URL**: https://natus250601.viewdns.net/project/kalisco/
- **서버 IP**: 222.100.103.227
- **개발 환경**: Spring Boot 기반 웹 애플리케이션

### 기술 스택
- **Backend**: Spring Boot 3.x (Java 17+)
- **Web Server**: Apache 2.4 (리버스 프록시)
- **Database**: PostgreSQL
- **Build Tool**: Gradle
- **Template Engine**: Thymeleaf (또는 정적 HTML)
- **Security**: Spring Security

---

## 🔧 네트워크 아키텍처

### 포트 구성
```
외부 요청 → Apache (80/443) → Spring Boot 애플리케이션
```

### 상세 포트 정보
- **80 (HTTP)**: Apache 리버스 프록시
- **443 (HTTPS)**: Apache SSL 터미네이션
- **9090 (내부)**: 캘리스코 Spring Boot 애플리케이션

### SSL/TLS 설정
- **인증서**: Let's Encrypt
- **자동 갱신**: certbot 설정
- **리다이렉션**: HTTP → HTTPS 자동 리다이렉션

---

## 📁 프로젝트 디렉토리 구조

### 루트 디렉토리
```
/project/kalisco/
├── doc/                    # 프로젝트 문서
│   ├── 01_프레임워크_구조_및_아키텍처.md
│   ├── 02_디렉토리별_프로그램_목록.md
│   └── 03_작업내역_및_TODO.md
├── API/                    # API 데이터 전송 프로그램
├── members/                # 회원관리 프로그램
├── product/                # 제품 관련 프로그램
├── orders/                 # 발주 관련 프로그램
├── sales/                  # 매출 관련 프로그램
├── promotion/              # 프로모션 관련 프로그램
├── organize/               # 조직 관련 프로그램
└── analyzer/               # 분석 관련 프로그램
```

### Spring Boot 프로젝트 구조 (예시)
```
/project/kalisco/
├── src/main/java/com/kalisco/
│   ├── KaliscoApplication.java
│   ├── controller/         # 컨트롤러
│   │   ├── ApiController.java
│   │   ├── MemberController.java
│   │   ├── ProductController.java
│   │   └── ...
│   ├── service/            # 서비스 레이어
│   │   ├── ApiService.java
│   │   ├── MemberService.java
│   │   └── ...
│   ├── repository/         # 리포지토리
│   │   ├── MemberRepository.java
│   │   └── ...
│   ├── entity/            # 엔티티
│   │   ├── Member.java
│   │   └── ...
│   └── config/            # 설정
│       ├── SecurityConfig.java
│       └── ...
├── src/main/resources/
│   ├── application.properties
│   ├── templates/         # 템플릿 파일
│   └── static/            # 정적 파일
│       ├── css/
│       ├── js/
│       └── project/kalisco/  # 프로젝트별 정적 파일
├── build.gradle           # Gradle 빌드 설정
└── gradlew                # Gradle Wrapper
```

---

## 🗄️ 데이터베이스 구성

### PostgreSQL 설정
- **호스트**: 127.0.0.1 (로컬)
- **데이터베이스명**: `kalisco_db` (예정)
- **사용자**: `myuser` / 비밀번호: `tngkrrhk`
- **포트**: 5432

### 주요 테이블 구조 (예정)
- **members**: 회원 정보
- **products**: 제품 정보
- **orders**: 발주 정보
- **sales**: 매출 정보
- **promotions**: 프로모션 정보
- **organizations**: 조직 정보
- **analytics**: 분석 데이터

---

## 🚀 서버 실행 및 관리 명령어

### 🎯 표준 재시작 프로세스 (프로그램 수정 후)

**프로그램 수정 후 반드시 다음 순서로 재시작:**

```bash
# 표준 재시작 스크립트 실행 (권장)
# 1. 서비스 다운 → 2. 빌드 → 3. JAR 파일로 서비스 시작
/var/www/html/project/kalisco/restart_kalisco.sh
```

**수동 실행 (단계별):**
```bash
# 1단계: 서비스 다운
sudo systemctl stop kalisco-service
pkill -f "java.*kalisco.*jar"

# 2단계: 빌드
cd /var/www/html/project/kalisco
./gradlew clean bootJar

# 3단계: JAR 파일로 서비스 시작
sudo systemctl start kalisco-service
```

### 개발 서버 실행 (개발/테스트용)
```bash
# 프로젝트 디렉토리로 이동
cd /var/www/html/project/kalisco

# Gradle 빌드 및 실행 (개발용)
./gradlew bootRun
```

### 프로덕션 서버 실행 (표준 프로세스: JAR 파일 방식)

#### 🎯 표준 재시작 프로세스 (권장)
```bash
# 프로그램 수정 후 표준 재시작 프로세스
# 1. 서비스 다운 → 2. 빌드 → 3. JAR 파일로 서비스 시작
/var/www/html/project/kalisco/restart_kalisco.sh
```

#### systemd 서비스 관리
```bash
# 서비스 시작
sudo systemctl start kalisco-service

# 서비스 상태 확인
sudo systemctl status kalisco-service

# 서비스 자동 시작 활성화
sudo systemctl enable kalisco-service

# 서비스 중지
sudo systemctl stop kalisco-service

# 서비스 재시작 (JAR 파일 재빌드 없이)
sudo systemctl restart kalisco-service
```

**서비스 파일 위치**: `/etc/systemd/system/kalisco-service.service`  
**실행 방식**: JAR 파일 직접 실행 (`java -jar`)  
**표준 재시작 프로세스**: `/project/kalisco/doc/05_표준_재시작_프로세스.md` 참고

### 서버 재시작 방법 (표준 프로세스)

#### 🎯 표준 재시작 프로세스 (프로그램 수정 후)
```bash
# 표준 재시작 스크립트 실행 (권장)
# 1. 서비스 다운 → 2. 빌드 → 3. JAR 파일로 서비스 시작
/var/www/html/project/kalisco/restart_kalisco.sh
```

이 스크립트는 다음을 자동으로 수행합니다:
1. ✅ 기존 서비스 종료 (systemd + 프로세스)
2. ✅ Gradle 빌드 실행 (`./gradlew clean bootJar`)
3. ✅ JAR 파일로 서비스 시작 (systemd)

#### 수동 재시작 (단계별)
```bash
# 1단계: 서비스 다운
sudo systemctl stop kalisco-service
pkill -f "java.*kalisco.*jar"

# 2단계: 빌드
cd /var/www/html/project/kalisco
./gradlew clean bootJar

# 3단계: JAR 파일로 서비스 시작
sudo systemctl start kalisco-service
```

#### JAR 파일만 재시작 (빌드 없이)
```bash
# JAR 파일이 이미 빌드되어 있는 경우
sudo systemctl restart kalisco-service
```

**자세한 비교**: `/project/kalisco/doc/04_서비스_실행_방법_비교.md` 참고

### 빌드 명령어
```bash
# 클린 빌드
./gradlew clean build

# 테스트 제외 빌드
./gradlew build -x test

# JAR 파일 생성
./gradlew bootJar
```

### 로그 확인
```bash
# 애플리케이션 로그 확인
tail -f logs/kalisco.log

# Spring Boot 로그 확인
tail -f logs/spring.log

# 에러 로그만 확인
tail -f logs/kalisco.log | grep ERROR
```

---

## 🔐 보안 설정

### Spring Security 설정
- **인증 방식**: 세션 기반 또는 JWT 토큰 기반
- **CORS 설정**: 필요한 도메인만 허용
- **CSRF 보호**: 활성화 (API는 예외 가능)

### 데이터베이스 보안
- **연결 암호화**: SSL/TLS 연결 권장
- **비밀번호 관리**: 환경 변수 또는 설정 파일로 관리
- **접근 제어**: 최소 권한 원칙 적용

---

## 📊 모니터링 및 관리

### 서버 상태 확인
```bash
# 포트 사용 상태 확인
netstat -tlnp | grep -E ":(9090)"

# Java 프로세스 확인
ps aux | grep -E "(java|gradle)" | grep kalisco

# 메모리 사용량 확인
free -h

# 디스크 사용량 확인
df -h
```

### 캘리스코 서비스 관리
```bash
# 서비스 상태 확인
sudo systemctl status kalisco-service

# 서비스 시작
sudo systemctl start kalisco-service

# 서비스 중지
sudo systemctl stop kalisco-service

# 서비스 재시작
sudo systemctl restart kalisco-service

# 서비스 자동 시작 활성화
sudo systemctl enable kalisco-service

# 서비스 자동 시작 비활성화
sudo systemctl disable kalisco-service

# 서비스 로그 확인
sudo journalctl -u kalisco-service -f
```

### Apache 설정 확인
```bash
# Apache 상태 확인
sudo systemctl status apache2

# Apache 재시작
sudo systemctl restart apache2

# Apache 설정 파일 확인
sudo cat /etc/apache2/sites-available/kalisco-proxy.conf
```

### 방화벽 설정
```bash
# 방화벽 상태 확인
sudo ufw status verbose

# 9090 포트 허용 (TCP)
sudo ufw allow 9090/tcp

# 방화벽 규칙 확인
sudo ufw status numbered

# 방화벽 활성화 (비활성화된 경우)
sudo ufw enable
```

**참고**: 9090 포트는 외부에서 직접 접근할 수 있도록 방화벽에서 허용되어야 합니다. 
Apache 리버스 프록시를 사용하는 경우 내부 포트이므로 방화벽 설정이 필요하지 않을 수 있습니다.

---

## 🔄 배포 프로세스

### 1. 코드 변경
```bash
# Git에서 최신 코드 가져오기
git pull origin main

# 변경사항 확인
git status
```

### 2. 빌드 및 테스트
```bash
# 빌드 실행
./gradlew clean build

# 테스트 실행
./gradlew test
```

### 3. 서버 재시작
```bash
# 기존 서버 종료
pkill -f "kalisco"

# 새 서버 시작
./gradlew bootRun --args='--server.port=9090' &
```

### 4. 배포 확인
```bash
# 로그 확인
tail -f logs/kalisco.log

# 서비스 상태 확인
curl http://localhost:9090/actuator/health
```

---

## 📝 환경 변수 설정

### application.properties 예시
```properties
# 서버 설정
server.port=9090
server.servlet.context-path=/project/kalisco

# 데이터베이스 설정
spring.datasource.url=jdbc:postgresql://127.0.0.1:5432/kalisco_db
spring.datasource.username=myuser
spring.datasource.password=tngkrrhk
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA 설정
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# 로깅 설정
logging.level.com.kalisco=DEBUG
logging.file.name=logs/kalisco.log
```

---

## 🐛 문제 해결

### 포트 충돌
```bash
# 포트 사용 중인 프로세스 확인
sudo lsof -i :9090
# 또는
netstat -tlnp | grep 9090

# 프로세스 종료 (PID 확인 후)
kill -9 <PID>

# Laravel 서비스가 9090 포트를 사용 중인 경우
sudo systemctl stop laravel-puzzle-service
```

**참고**: 9090 포트는 캘리스코 프로젝트 전용 포트입니다.
기존 Laravel 퍼즐 서비스(`laravel-puzzle-service`)는 중지되었으며, 
캘리스코 서비스(`kalisco-service`)가 9090 포트를 사용합니다.

### 빌드 실패
```bash
# Gradle 캐시 삭제
./gradlew clean

# 의존성 다시 다운로드
./gradlew build --refresh-dependencies
```

### 데이터베이스 연결 실패
```bash
# PostgreSQL 상태 확인
sudo systemctl status postgresql

# 데이터베이스 연결 테스트
PGPASSWORD=tngkrrhk psql -h 127.0.0.1 -U myuser -d kalisco_db
```

---

## 📚 참고 문서

- [Spring Boot 공식 문서](https://spring.io/projects/spring-boot)
- [PostgreSQL 공식 문서](https://www.postgresql.org/docs/)
- [Gradle 공식 문서](https://docs.gradle.org/)

---

**작성일**: 2025-12-08  
**작성자**: Cursor AI Assistant  
**버전**: 1.0
